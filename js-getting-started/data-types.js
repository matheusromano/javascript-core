/* Objetcs also called by me a dictionarie 
let person = {
  firstName: "John",
  lastName: " Adams",
};

showMessage(person.firstName + person.lastName)
*/

/* 
Undefined and Null values
if the var has not a value we say that is undefined, 
but the best practice is to set to null instead undefined 
let saved;
saved = null;

showMessage(saved);
console.log(saved);*/

/* Boolean Values
let saved = true;
saved = !saved;
showMessage(saved); */

/*let amount = 123;
amount = amount.toString();
showMessage(typeof amount);

// if put a letter in the in the bagin will throw "nan" or else will be ok
let amount2 =  Number.parseFloat('A123');
showMessage( amount2);*/

/*let name = 'Matheus'
let message = name  + " is a boss"

showMessage(message.length);
console.log(typeof message); */

/*let amount = 20 + (-2);

showMessage(amount);*/

/* if you put the + opertar after the var 
 the increment will occurs after the code runs
 let price = 1.1 + 1+3;
showMessage(price++);
console.log(price );*/

// typeof return the type of the variable
// showMessage(typeof price);
